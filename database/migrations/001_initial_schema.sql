-- Migration: 001_initial_schema
-- Description: Initial schema (tables, indexes, RLS)
-- Run this after schema.sql if using incremental migrations

-- This file can be used to replay schema changes.
-- For Supabase, applying database/schema.sql once is usually enough.
-- Migrations here are for versioned changes (e.g. new columns, new tables).

SELECT 1;
